# ── Cloudflare Pages (Statische App) ─────────────────────────────────────────
name = "teachsmarter"
pages_build_output_dir = "."

# Deploy (IMMER --branch production, sonst landet es nur in Preview):
# wrangler pages deploy . --project-name=teachsmarter --branch=production
